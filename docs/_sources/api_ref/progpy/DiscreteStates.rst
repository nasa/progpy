Discrete States
================

:term:`Discrete States<discrete state>` are a representation that can only occupy one of a finite set of predefined values (e.g., engine gear or switch). Discrete states are initialized using the function :py:func:`progpy.create_discrete_state`, described below.

.. autofunction:: progpy.create_discrete_state
